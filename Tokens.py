# Tokens are the smallest units in Python code: keywords, identifiers, literals, operators, and delimiters.
# Each line of code is broken into tokens by the interpreter for parsing and execution.

x = 5
print(x + 2)
